# https://tinylog.org/configuration
# Levels: trace < debug < info < warning < error
tinylog.level = info

# Default format
#   tinylog.format = {date:yyyy-MM-dd HH:mm:ss} [{thread}] {class}.{method}()\n{level}: {message}
tinylog.format = {line}: {class}.{method}  {level}: {message}